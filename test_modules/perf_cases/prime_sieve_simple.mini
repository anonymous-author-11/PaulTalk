import primesieve;

limit = 2_000_000;
sieve = SimplePrimeSieve{limit};
sieve.generate_primes();
count = sieve.count_primes();

if count == 148933 {
    IO.print("ok");
} else {
    IO.print("bad");
}
