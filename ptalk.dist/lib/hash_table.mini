import swiss_table_v3;
import io;

map = {5:77, 8:34, 15:99};
val = map.[8];
IO.print(val);

stringmap = {"foo":"bar", "baz":"qux", "hello":"world"};

for (key, value) in stringmap {
    IO.print(key);
    IO.print(value);
}

def say_apple() {
    IO.print("apple");
}

def say_banana() {
    IO.print("banana");
}

def say_coconut() {
    IO.print("coconut");
}

fmap = {"a":say_apple, "b":say_banana, "c":say_coconut};
func = fmap.["c"];
func.call();