import std;

def add_translation(translation : String) -> String {
	str = "Computer in Chinese is ";
	return str.extend(translation);
}

IO.print(add_translation("电脑"));

IO.print(['a', 'b', 'c', 'd'] of Character into String);

nilable : String? = nil;