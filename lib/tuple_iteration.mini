import std;

triple = (1,2,3);
mapped = (triple * triple).map((x : i32) => { x * 2; });
for i in mapped { IO.print(i); }

bools = (10 from 0) < (10 of 5);

IO.print(bools.[3]);
IO.print(bools.[7]);

for bool in bools { IO.print(bool); }