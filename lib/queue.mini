import list;

class Queue[T] extends List[T] {

    abstract def dequeue() -> T?

    def enqueue(item : T) -> Self {
        return self.append(item);
    }

    def peek() -> T? {
        return self.first();
    }
}
